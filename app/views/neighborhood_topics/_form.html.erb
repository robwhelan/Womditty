<%= form_for(@neighborhood_topic) do |f| %>
  <% if @neighborhood_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@neighborhood_topic.errors.count, "error") %> prohibited this neighborhood_topic from being saved:</h2>

      <ul>
      <% @neighborhood_topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
