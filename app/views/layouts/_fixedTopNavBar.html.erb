
<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
	<%= link_to 'Womditty', root_path, :class => 'navbar-brand' %>
  </div>

  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
    <ul class="nav navbar-nav">
      <li>
      	<%= link_to neighborhoods_path do %>
			<span class="glyphicon glyphicon-home"></span> Neighborhoods
			<% end %>
      </li>
      <li>
			<%= link_to photos_path do %>
				<span class='glyphicon glyphicon-camera'></span> Photos
			<% end %>
	  </li>
      <li>
			<%= link_to posts_path do %>
				<span class='glyphicon glyphicon-comment'></span> Posts
			<% end %>
	  </li>
    </ul>
		<% @search = Post.search(params[:q]) %>
		<%= search_form_for @search, :class => "navbar-form navbar-left" do |f| %>
      	<div class="form-group">
		<%= f.text_field :body_cont, :class => 'form-control', :placeholder => 'Search' %>
      	</div>
		<%= f.submit 'Go', :class => 'btn btn-default' %>
		<% end %>

	<% if user_signed_in? %>
    <ul class="nav navbar-nav navbar-right">
		<%= render 'shared/user_stuff' %>
    </ul>
	<% end %>
  </div><!-- /.navbar-collapse -->
</nav>
