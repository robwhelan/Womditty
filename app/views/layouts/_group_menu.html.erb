<h4><%= @forum.name %></h4>
	<% @groups.each do |group| %>
		<%= link_to group.name, pages_chat_path(:group => group, :forum => @forum, :page => '1'), :class => "btn btn-primary btn-lg btn-block active myGroupsButton" %>
	<% end %>