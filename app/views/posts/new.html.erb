<img width="30" src="#{<%= current_user.profile_image %>}" />

<%= form_tag(posts_path, :remote => true) do %>
	<%= text_field(:post, :body) %>
	<%= hidden_field(:post, :user_id, :value => current_user.id ) %>
	<%= submit_tag 'Comment', :class => 'btn' %>
<% end %>


