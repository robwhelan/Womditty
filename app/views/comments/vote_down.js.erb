$('li#commentLikesCount' + '<%= "#{@comment.id}" %>').text('<%= "#{@comment.plusminus}" %>');
$('i#commentHeart' + '<%= "#{@comment.id}" %>').parent().parent().attr("class", "btn");
$('i#commentHeart' + '<%= "#{@comment.id}" %>').parent().parent().attr("href", '<%= vote_up_comment_path(@comment) %>');
$('li.commentUserVoteCount' + '<%= "#{@comment.user.id}" %>').text('<%= "#{@comment.user.karma}" %>');